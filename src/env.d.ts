/**
 * Type declarations for compile-time constants injected by Vite's `define`.
 */

/** Application version string, injected from package.json at build/dev time. */
declare const __APP_VERSION__: string
